import { SearchcenterPipe } from './searchcenter.pipe';

describe('SearchcenterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcenterPipe();
    expect(pipe).toBeTruthy();
  });
});
